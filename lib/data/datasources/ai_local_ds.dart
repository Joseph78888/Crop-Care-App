class AiLocalDataSource {
  Future<String> runInferenceOnImagePath(String imagePath) async {
    // Placeholder for TensorFlow Lite inference
    await Future<void>.delayed(const Duration(milliseconds: 400));
    return 'Healthy';
  }
}


