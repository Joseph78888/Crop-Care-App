class ApiRemoteDataSource {
  Future<String> sendImageAndGetDiagnosis(String imagePath) async {
    // Placeholder for backend call
    await Future<void>.delayed(const Duration(milliseconds: 400));
    return 'Healthy';
  }
}


